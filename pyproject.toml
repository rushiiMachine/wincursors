[project]
name = "wincursors"
version = "1.0.0"
description = "Windows 10 cursor theme for Linux (xcursor + hyprcursor) built with accurse"
requires-python = ">=3.13"
dependencies = [
    "accurse>=0.1.0",
]

[tool.uv]
package = false
